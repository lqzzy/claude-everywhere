# Claude Remote 重构方案

> 目标：**手机控制 claude 对话** + **手机建的对话能在电脑终端 `resume` 续聊，且来回交接不分叉**。
> 原则：最简洁优雅，砍掉一切为"双向接管终端"而生的复杂注入。

## ✅ 实施状态（2026-06-14 已完成 + 真机验证）

| 部分 | 状态 | 验证 |
|---|---|---|
| Server 重构（无状态 + 每轮 resume） | ✅ 完成 | `tsc` 通过；`verify.ts` 两轮 resume + 落盘；终端 resume 不分叉；手机↔电脑交接 Q_A/Q_B 真机通过 |
| 删注入/镜像（hostWrapper/transcriptMirror/inputQueue） | ✅ 完成 | 依赖去 node-pty/@xterm；1063→679 行 |
| Kotlin/Compose App（`kotlin-app/`） | ✅ 完成 | `assembleDebug` 成功；真机安装启动无崩；**连上 server、实时渲染真实会话卡片**（截图实锤） |

**仍可加（非阻塞）**：列表多选批量（已留 TODO）、置顶持久化（当前本地内存）、服务器地址设置页（免改源码切 IP）、推送通知、独立 APK 签名。
旧 Expo `app/` 暂保留，可在确认 Kotlin 版满意后归档。

## 一、决策（已确认）

| 项 | 决定 |
|---|---|
| 会话模型 | **每轮 resume、server 无状态**（真相在磁盘 `.jsonl`） |
| App | **原生 Kotlin + Jetpack Compose**（仅 Android） |
| TranscriptMirror（监听全部会话） | **删除**，改为按需读单个 `.jsonl` |
| server 语言 | 仍 **Node/TS**（Agent SDK 只有 JS 版，无法换 Kotlin） |
| 权限 | 默认 `bypassPermissions` 自动放行（手机=可信，控制自己电脑）；可一行开关恢复审批 |

## 二、为什么这样最简洁

现状三套东西，复杂度天差地别：

- `sdkSession.ts`(267) — 核心，Agent SDK 驱动 claude，真流式。**保留并改造**。
- `transcriptMirror.ts`(228) — 监听 `~/.claude/projects/**`，镜像所有会话。**删**。
- `hostWrapper.ts`(199) — PTY 跑 claude + xterm 抓屏 + 正则解析 spinner + 把手机消息敲进 TUI。**这就是"复杂注入"，整个删**。

**关键认知**：需求 #2（终端 resume）不用"做"——Agent SDK 的 `query()` 底层就是拉起 `claude`，会话照常写进 `~/.claude/projects/<cwd>/<session_id>.jsonl`，终端 `claude --resume <id>` 天生能续。我们要做的是**删掉多余的，并把会话状态从 server 内存挪回磁盘**，这样手机/电脑永远以同一份磁盘 transcript 为准，来回交接不分叉。

## 三、Server 重构（Node/TS）

### 删除
- `hostWrapper.ts`、`transcriptMirror.ts`、`inputQueue.ts`
- `protocol.ts` 里所有 `host.*`（register/unregister/turn/delta/inject）
- `server.ts` 里 `hostSessions`、`startMirror()`、host.* 分支
- 依赖：`node-pty`、`@xterm/headless`、`@xterm/addon-serialize`、`package.json` 的 `rc` 脚本

### 新结构（约 4 个文件，~600→~350 行）

```
server/src/
  protocol.ts      线协议（删 host.*，SessionSummary 增 claudeSessionId）
  store.ts         appId → 会话元数据指针表，落地 ~/.claude-remote/sessions.json（重启不丢）
  turn.ts          runTurn(appId, text, emit)：每轮 resume 跑一次 query，结束即退
  history.ts       readHistory(claudeId, cwd)：按需读单个 .jsonl（复用旧 mirror 的解析）
  server.ts        WebSocket 入口 + 命令分发（瘦身版）
```

### 核心流程：每轮 resume

```ts
// turn.ts —— 一条消息 = 一次性 query，跑完即退，不常驻
async function runTurn(appId, text, emit) {
  const meta = store.get(appId);                    // 取该会话"最新 claudeId"（首轮为空）
  const q = query({ prompt: text, options: {
    cwd: meta.cwd, model: meta.model,
    resume: meta.claudeSessionId,                   // ← 首轮 undefined；之后续最新
    includePartialMessages: true,
    permissionMode: "bypassPermissions",
  }});
  activeTurns.set(appId, q);                        // 仅本轮存句柄，供 interrupt
  for await (const msg of q) {
    if (msg.type === "system" && msg.subtype === "init")
      store.update(appId, { claudeSessionId: msg.session_id }); // ← 关键：每轮以 init 的 id 为准
    mapToWire(msg, appId, emit);                    // 复用现有 handleStream/Assistant/Result 映射
  }
  activeTurns.delete(appId);                        // 进程结束 → 文件释放，电脑可随时接管
}
```

> 无论 SDK 续用同 id 还是 resume 开新 id，都以**每轮 init 返回的 `session_id`** 更新指针——两种行为都健壮。

### 命令行为
- `session.start` → 建 appId、写元数据、`runTurn`（不带 resume）
- `session.input` → `runTurn`（带 resume）；同会话同时只允许一轮，进行中再发→排队或拒绝
- `session.subscribe` → `readHistory(latestClaudeId, cwd)` 读磁盘最新真相 → 回 history
  （**这就是 Q_A 的答案**：电脑 resume 后聊的几轮，手机一打开就能看到）
- `session.interrupt` → `activeTurns.get(appId)?.interrupt()`
- `session.delete` / `session.archive` → 改 store + 广播（transcript 文件保留，仅从列表移除）
- 会话卡片下发 `claudeSessionId`+`cwd` → App 显示可复制的 `cd <cwd> && claude --resume <id>`

### 手机↔电脑交接（最终行为）
| 场景 | 结果 |
|---|---|
| 手机建会话 | 写入 `~/.claude/projects/.../<id>.jsonl` |
| 电脑 `claude --resume <id>` 续聊 | 直接续上（手机进程已退，无冲突） |
| **Q_A**：电脑聊完反映回手机 | 手机重开/刷新该会话即看到电脑那几轮 ✅（非电脑打字时实时） |
| **Q_B**：回手机再聊 | 从最新 `.jsonl` resume，电脑那几轮在上下文里，**不分叉** ✅ |

## 四、App 重构（Kotlin + Jetpack Compose）

全新 Kotlin 工程，弃用 Expo/RN（连带摆脱刚才那种 RN 原生 NDK/CMake 重型构建）。

### 技术选型
- 网络：**OkHttp WebSocket**，自动重连，事件 → `Flow<ServerEvent>`
- 序列化：**kotlinx.serialization**，`ServerEvent`/`ClientCommand` 用 sealed class + `@JsonClassDiscriminator("t")` 对齐 protocol.ts
- 状态：`ViewModel` + `StateFlow`（移植现 zustand store 的逻辑：sessions / 当前消息 / 流式缓冲 / 心跳）
- UI：Compose，复刻 **d8 通透液态**
  - 玻璃：`Modifier.blur()`(RenderEffect, **minSdk 31**) 或 **Haze** 库做真实背景模糊
  - 暖白渐变底 + 模糊光斑 blob、珊瑚渐变 brush、镜面高光、暖棕柔阴影
  - 配色：移植 `theme.ts` 到 Compose `Color` 调色板
- 渲染：server 已下发结构化 `ContentBlock`（text/thinking/tool_use/tool_result），**直接按块渲染**，几乎不用解析 markdown

### 屏幕
1. `SessionListScreen` —— 即 `design/sessions.html` 那版：需处理分区 + 时间流、搜索、时间/位置切换、置顶、左滑归档/删除、多选批量
2. `SessionScreen` —— 对话：珊瑚用户气泡、玻璃 AI 卡、思考/工具折叠块、代码块、diff、打字机、token 心跳行、底部珊瑚发送

### 构建优势
纯 Compose、无 RN 原生模块 → gradle 构建快、无 Metro/node_modules、单 Kotlin 语言栈。

## 五、实施顺序

1. **Server 先行**（小、解锁一切）：删旧模块 → 建 store/turn/history → 用 `sendcmd.ts` 改的小客户端验证每轮 resume + 交接
2. **Kotlin App**：建工程 → 协议+WS 客户端 → 会话列表(d8) → 对话页 → 玻璃打磨
3. App 达到 parity 后，归档旧 `app/`(Expo)

## 六、待定小项（已给默认，可改）
- 权限：默认 `bypassPermissions` 自动放行；若要"危险操作弹手机审批"，保留 `permission.request` 一条链路即可
- 指针存储：`~/.claude-remote/sessions.json`
- minSdk：建议 31（原生 blur）；要更低则用 Haze 软件回退
