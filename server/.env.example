# 监听端口与地址(0.0.0.0 让 Tailscale 上的手机能连)
PORT=4000
HOST=0.0.0.0

# 鉴权 token(必填!手机连接时要带上)。用 `openssl rand -hex 32` 生成
AUTH_TOKEN=

# App 发起新会话时的默认工作目录
DEFAULT_CWD=/Users/qili

# 可选:默认模型(留空则用 claude 的默认)
# DEFAULT_MODEL=opus
